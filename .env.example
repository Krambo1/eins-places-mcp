# Google Places API (New) key. Restrict to "Places API (New)" only in GCP console.
GOOGLE_PLACES_API_KEY=

# Bearer token claude.ai will send on every MCP request.
# Generate a long random string (e.g. `openssl rand -hex 32`).
# This is your only line of defense; pick something strong.
MCP_BEARER_TOKEN=
